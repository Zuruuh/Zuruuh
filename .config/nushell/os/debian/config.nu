module debian {
    export alias phpversionmanager = pvmp
    export alias phpvm = pvm
    export alias pvm = sudo update-alternatives --config php

    export alias wslconfig = sudo nvim /etc/wsl.conf
    export alias open = explorer.exe
    export alias gsd = git switch dev
}

